if we don't put the file in the api folder we can call the endpoint without specifiing the /api.

We can use one methode for each folder.The best practice is to add the methode as a suffix in the name of the file and this file will accept only the methode that we add as suffix.